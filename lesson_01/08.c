#include <stdio.h>
#include <stdlib.h>

int input(){
    int n;
    double a;
    n = scanf("%lf", &a);
    if(n != 1){
        return 1;
    }
    if
    return a;
}

void main8(){
    int a;

    a = input();

    printf("%d", a);
}
